# Portfolio screen QA — 2026-09-22

Reviewed the production build from `54c273a` locally in headless Chrome. The review covered both `?lang=es` and `?lang=en` at 320 × 844, 390 × 844, 768 × 900, and 1440 × 900. Full-page captures at 320 and 1440 pixels in both languages were inspected from hero through footer.

| Check | Result |
| --- | --- |
| Responsive layout | No horizontal page overflow at any tested width; the hero heading stayed inside the viewport. |
| Images and navigation | No failed images or missing in-page anchor targets on the rendered page. |
| Browser errors | No uncaught page errors in the eight language/viewport combinations. |
| Mobile navigation | Menu opened at 390 px; choosing Experience navigated to the section and closed the menu. |
| Language switch | Spanish-to-English switch updated the document language and rendered English experience text. |
| Project details | Drawer opened; Escape closed it and returned focus to its trigger. |
| Keyboard and print | The first keyboard-focused link had a visible CSS outline; reveal items had full opacity in print media. |
| External links | Links opening new tabs included `rel="noopener"`. Destination availability was not tested. |

The experience title, in-progress engineering degree, and LFCA acronym were also checked against the current profile facts and ES/EN CV copies. The portfolio uses the confirmed formal title in both languages.

The review found no additional screen defect that warranted a code change. It verifies the local production build, not the live deployment or behavior in every browser and assistive technology.
